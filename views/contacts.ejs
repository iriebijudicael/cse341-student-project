<!-- 
<%- include('partials/header') %>

    <main>
        <div class="view-contacts">
            <h2>Your Contacts</h2>
            <p>A list of all users currently stored in the database.</p>

            <table class="contacts-table">
                <thead>
                    <tr>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Email</th>
                        <th>Favorite Color</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% contacts.forEach(function(contact) { %>
                        <tr>
                            <td><%= contact.firstName %></td>
                            <td><%= contact.lastName %></td>
                            <td><%= contact.email %></td>
                            <td><%= contact.favoriteColor %></td>
                            <td>
                                <a href="/contacts/<%= contact._id %>" class="view-btn">View Details</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table> 
        
            <div class="actions">
                <a href="/" class="add-btn">Add New Contact</a>
            </div>
        </div>
    </main>

<%- include('partials/footer') %> -->